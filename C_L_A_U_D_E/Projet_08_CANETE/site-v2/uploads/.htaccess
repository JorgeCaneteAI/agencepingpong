# Deny all direct access and script execution
<FilesMatch ".*">
    Require all denied
</FilesMatch>

# Disable PHP execution
<FilesMatch "\.ph(p[3457]?|t|tml)$">
    Require all denied
</FilesMatch>

php_flag engine off
